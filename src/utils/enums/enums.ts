export enum OrderStatusEnum {
    Created,
    InProgress,
    Finished,
    Canceled,
    Excluded
}

export enum PaymentOfTypeEnum {
    Money,
    PIX,
    DebitCard,
    CreditCard,
    Debitor,
    ExtraMoney
}